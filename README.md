# homebrew-prediup

Homebrew tap for the PrediHermes local installer.

## Install

```bash
brew tap nativ3ai/prediup
brew install predihermes
```

Or directly:

```bash
brew install nativ3ai/prediup/predihermes
```

Compatibility formula:

```bash
brew install nativ3ai/prediup/prediup
```
